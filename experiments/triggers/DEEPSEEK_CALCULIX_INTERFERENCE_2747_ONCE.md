# DeepSeek CalculiX interference diagnosis one-time trigger

This immutable marker triggers exactly one trusted push run of `.github/workflows/deepseek-calculix-interference-loop.yml` on `exp/deepseek-crack-open-discovery`.

The run uses the repository `ds` Environment, creates one DeepSeek client, permits at most three HTTP requests with SDK retries disabled, executes real CalculiX C3D20R contact variants, and uploads an artifact-only decision trace and paper.

Do not edit, rename, or delete this marker after its first push. Source, documentation, and frozen paper commits do not match the paid workflow path.
