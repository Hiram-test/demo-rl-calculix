name: MeshPilot batch-transfer PSO VM

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  hold:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Batch PSO workflow temporarily paused while one coherent update is assembled."
