# WMVLA-4WAY-P1 快速执行事后修正

- 授权日期：2026-08-30（UTC）
- 授权内容：性能与参考一致性门槛只作报告判据，不再阻塞后续实验执行。
- 修正性质：本文件是在单个训练案例的诊断性参考网格完成第 0、1、2 层以后建立，属于明确披露的事后执行修正，不冒充预注册条款。
- 授权标识：`user_authorized_nonblocking_2026-08-30`

## 保持不变的边界

1. 案例清单、随机种子、训练/验证/测试划分、预算、主比较点和方法定义不变。
2. 测试集继续封存；只允许在冻结完成后按案例编号升序执行一次完整的 16 例盲测。
3. 方程预算不得超限，所有原生网格、CalculiX 失败和不完整轨迹必须如实保留。
4. 原始日志、输入、哈希、计时和资格状态必须留存；不得把未达标结果写成通过或收敛。
5. Reference B 只能在各方法完成动作以后加载并作事后计分，不能进入在线决策。

## 非阻塞参考模式

1. 快速执行命令显式使用 `allow_unqualified=true` 和 `expedited_levels=2`；默认严格模式仍使用预注册的最多 6 层计划。
2. 两个固定参考层都原生成功时，仍按原式计算能量与 QoI 的相对差异，并继续用 0.5% 判断 `qualification`。
3. 若两项差异都不超过 0.5%，状态沿用严格协议的 `complete`，并明确记录 `qualification=true`，较细层为 Reference B。
4. 若两个参考层都成功但任一差异超过 0.5%，较细层可作为操作性 Reference B，状态必须为 `complete_unqualified`，且 `qualification=false`。
5. 任一参考层发生网格、CalculiX、数值或完整性失败时，整个案例仍为失败；不得回退到此前成功层冒充操作性 Reference B。
6. 严格加载接口默认拒绝未达资格的参考；只有冻结配置和执行命令同时显式允许时才能使用，并须在逐案例记录与首页报告中标明。

## 非阻塞性能门槛

原方案中的几何均值、bootstrap 上界、胜率、P95、QoI、预算违规数和主动动作比例继续完整计算和报告，但不再决定是否继续完成冻结后的其余实验、消融、制图、归档和推送。最终结论必须分别给出数值与通过状态，不能因门槛改为非阻塞而省略失败项。

## 修正前已知信息

建立本修正时，仅有一个训练案例的独立诊断试算结果：第 0→1 层能量/QoI 相对差异约为 4.56%，第 1→2 层约为 6.01%，均未达到 0.5%。该诊断位于正式结果根目录之外；正式测试集尚未访问，正式方法结果尚未产生。
